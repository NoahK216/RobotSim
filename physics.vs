void masterAcceleration(Robot *robot, int isW, int isS);
void masterPhysics(Robot *robot, char *buffer, int buffer_count);
void masterPosition(Robot *robot);
void masterVelocity(Robot *robot, double accelMagnitude);
